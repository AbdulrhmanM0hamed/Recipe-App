class Category {
  final String title;
  final String
      imagePath; // path for the SVG image or any other relevant properties
  final int mealCount; // number of meals in the category

  Category({
    required this.title,
    required this.imagePath,
    required this.mealCount,
  });
}
